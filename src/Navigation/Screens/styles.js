import {
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor: "grey"
    },
    list :
    {
        alignItems: "center"
    }

})

export default styles